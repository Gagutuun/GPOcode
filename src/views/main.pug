extends layout

block content
  .content
    .logo
      a.button(href='/')
        img.mainImg(src='../images/logo.png')
    .forma
      form#uploadForm(ref='uploadForm', action='/upload', method='post', enctype='multipart/form-data')
        input.mainInput(type='file', name='sampleFile', accept=".pdf" required)
        input.mainInput.loading(type='submit', value='Загрузить')
        input(type='date', name='DateOfUpload', required)