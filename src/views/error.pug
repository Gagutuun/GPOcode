extends layout

block content
  .content
    .logo
      a.button(href='/')
        img.mainImg(src='../images/logo.png')
    h1= "Произошла ошибка!"      
    h1= message
    h2= error.status
    pre #{error.stack}
